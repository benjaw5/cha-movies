CREATE UNIQUE INDEX movies_idx ON  movies ( id, title, year, director);

CREATE UNIQUE INDEX stars_idx ON  stars ( id, name, birthYear);

CREATE UNIQUE INDEX genres_idx ON  genres ( id, name);

CREATE UNIQUE INDEX ratings_idx ON  ratings ( Movieid, rating, numVotes);

CREATE UNIQUE INDEX genres_in_movies_idx ON  genres_in_movies ( genreId, movieId);

CREATE UNIQUE INDEX stars_in_movies_idx ON  stars_in_movies ( starId, movieId);